const routes = {
    register: "/register",
    login: "/login",
    dashboard: "/dashboard",
    index: "/"
}

export const authroutes = [routes.login, routes.register, routes.index]

export default routes;